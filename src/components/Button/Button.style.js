import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fc8911',
    alignItems: 'center',
    padding: 10,
    margin: 10,
    borderRadius: 10,
  },
  title: {
    color: 'black',
    fontWeight: 'bold',
    fontSize: 15,
    textAlign: 'center',
  },
});
