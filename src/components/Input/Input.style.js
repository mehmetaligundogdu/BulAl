import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 5,
    margin: 10,
    marginTop: 0,
    backgroundColor: '#A7D8F4',
    borderRadius: 20,
  },
});
