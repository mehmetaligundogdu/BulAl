/**
 * @format
 */
import {AppRegistry} from 'react-native';
import {Wrapped} from './src/Wrapped';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Wrapped);
